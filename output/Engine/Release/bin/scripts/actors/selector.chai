class selector{
    attr widget

    def selector(name, origin, size){
        this.widget := stage.create_actor(name)
        this.widget.set_origin(origin)

        this.widget.add_model(name, origin, size, "selector.json")
    }

    def get(){
        return this.widget
    }

}

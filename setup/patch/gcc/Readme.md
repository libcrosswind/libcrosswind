This is a fix for mingw to use stoul, stoi, to_string and many other string manipulation methods. Required for some modules in the framework. 

This patch is applied automatically after installing all other dependencies and running build.bat inside the setup/win32 folder.